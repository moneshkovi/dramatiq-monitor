# Pinned to versions verified working in this project's development
# environment (conda env "dramatiq", Python 3.11). Ranges in pyproject.toml
# are intentionally looser for downstream flexibility; use this file when you
# want the exact tested combination without going through the pyproject
# extras. Install with: pip install -r requirements.txt
starlette==0.45.3
jinja2==3.1.6
redis==5.0.7
uvicorn==0.35.0
