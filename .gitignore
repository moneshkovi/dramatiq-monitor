.venv/
__pycache__/
*.egg-info
.pytest_cache
dist/
build/

# Internal build-session dump — references the private deployment this repo
# must stay agnostic of. Public docs live in docs/ instead. See SESSION_NOTES.md
# itself for why.
SESSION_NOTES.md

# Internal-only docs — keep local, not part of the public repo.
docs/
